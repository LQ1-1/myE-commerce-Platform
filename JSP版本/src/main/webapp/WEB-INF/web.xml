<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <!-- 配置自定义编码过滤器 -->
    <filter>
        <filter-name>CustomCharacterEncodingFilter</filter-name>
        <filter-class>com.lqh.ebuyplt_jsp.Controller.Filter.CustomCharacterEncodingFilter</filter-class>
        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>
    </filter>
    <filter-mapping>
        <filter-name>CustomCharacterEncodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <servlet>
        <servlet-name>JerseyServlet</servlet-name>
        <servlet-class>org.glassfish.jersey.servlet.ServletContainer</servlet-class>
        <!-- 扫描你的接口包（必须和BasicController所在包一致） -->
        <init-param>
            <param-name>jersey.config.server.provider.packages</param-name>
            <param-value>com.lqh.ebuyplt_jsp.Controller.JSPVersionController</param-value>
        </init-param>
        <!-- 完整CORS配置（解决OPTIONS预检403） -->
        <init-param>
            <param-name>jersey.config.server.cors.enabled</param-name>
            <param-value>true</param-value>
        </init-param>
        <init-param>
            <param-name>jersey.config.server.cors.allowed Origins</param-name>
            <param-value>*</param-value> <!-- 允许所有来源 -->
        </init-param>
        <init-param>
            <param-name>jersey.config.server.cors.allowed Methods</param-name>
            <param-value>GET, POST, OPTIONS</param-value> <!-- 允许OPTIONS预检 -->
        </init-param>
        <init-param>
            <param-name>jersey.config.server.cors.allowed Headers</param-name>
            <param-value>Content-Type, Accept</param-value> <!-- 允许前端请求头 -->
        </init-param>
        <init-param>
            <param-name>jersey.config.server.cors.exposed Headers</param-name>
            <param-value>Content-Type</param-value>
        </init-param>
        <init-param>
            <param-name>jersey.config.server.cors.supported Headers</param-name>
            <param-value>Content-Type</param-value>
        </init-param>
        <init-param>
            <param-name>jersey.config.server.cors.max Age</param-name>
            <param-value>3600</param-value> <!-- 预检缓存1小时 -->
        </init-param>
        <load-on-startup>1</load-on-startup>
    </servlet>

    <!-- 映射所有请求到Jersey -->
    <servlet-mapping>
        <servlet-name>JerseyServlet</servlet-name>
        <url-pattern>/*</url-pattern>
    </servlet-mapping>

    <!-- 欢迎页 -->
    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file-list>
    </welcome-file-list>

</web-app>